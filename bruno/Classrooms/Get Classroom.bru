meta {
  name: Get Classroom
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/v1/classrooms/{{classroomId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq {{classroomId}}
}

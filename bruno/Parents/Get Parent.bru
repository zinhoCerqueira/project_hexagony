meta {
  name: Get Parent
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/v1/parents/{{parentId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq {{parentId}}
}

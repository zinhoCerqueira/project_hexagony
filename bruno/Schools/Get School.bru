meta {
  name: Get School
  type: http
  seq: 4
}

get {
  url: http://localhost:8080/api/v1/schools/{{schoolId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq {{schoolId}}
}

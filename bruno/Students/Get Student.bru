meta {
  name: Get Student
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/v1/students/{{studentId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq {{studentId}}
}
